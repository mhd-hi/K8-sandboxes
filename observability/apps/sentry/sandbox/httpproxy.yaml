apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: sentry
spec:
  virtualhost:
    fqdn: sentry.sandbox.cedille.club
    tls:
      secretName: tls-delegation/sandbox-tls
  routes:
    - conditions:
        - prefix: /
      services:
        - name: sentry-nginx
          port: 80
      timeoutPolicy:
        idle: 600s
        response: 600s
