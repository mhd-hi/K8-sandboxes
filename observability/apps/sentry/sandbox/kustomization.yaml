apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sentry

resources:
  - ../base
  - clickhouse.yaml
  # - httpproxy.yaml

helmCharts:
  # - name: altinity-clickhouse-operator
  #   version: "0.26.0"
  #   repo: "https://helm.altinity.com"
  #   releaseName: clickhouse-operator
  #   namespace: sentry
  #   valuesFile: clickhouse-operator-values.yaml

  - name: sentry
    version: "29.5.1"
    repo: "https://sentry-kubernetes.github.io/charts"
    releaseName: sentry
    namespace: sentry
    valuesFile: sentry-values.yaml